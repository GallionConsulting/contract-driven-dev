{
  "name": "contract-driven-dev",
  "version": "3.1.0",
  "description": "Contract-Driven Development (CDD) command system for Claude Code — enforce interface contracts across multi-session builds",
  "bin": {
    "contract-driven-dev": "bin/install.js"
  },
  "files": [
    "bin",
    "commands",
    "cdd"
  ],
  "engines": {
    "node": ">=16.7.0"
  },
  "keywords": [
    "claude-code",
    "contract-driven-development",
    "cdd",
    "ai-development",
    "multi-session",
    "interface-contracts"
  ],
  "author": "",
  "license": "MIT",
  "scripts": {
    "version": "node -e \"const fs=require('fs');const v=require('./package.json').version;const r=fs.readFileSync('README.md','utf8');fs.writeFileSync('README.md',r.replace(/version-[0-9]+\\\\.[0-9]+\\\\.[0-9]+-blue/,'version-'+v+'-blue'));\" && git add README.md"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/GallionConsulting/contract-driven-dev.git"
  }
}
