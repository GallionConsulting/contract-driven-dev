# Contract Change Log

> This file should ideally stay empty. Every entry represents a planning failure.
>
> If a change is necessary, use `cdd:contract-change` to formally request it.
> The process is intentionally heavyweight — the friction is proportional to the impact.
